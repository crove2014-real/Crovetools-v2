# Crove Tools v2.1
纯 Cloudflare Pages + Functions，无需 KV。

## Pages 设置
- Framework preset: None
- Build command: 留空
- Build output directory: `.`
- Root directory: 留空

上传后仓库根目录必须直接看到 `index.html`、`app.js`、`style.css`、`functions/`。
