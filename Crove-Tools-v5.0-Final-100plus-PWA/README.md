# Crove Tools v5.0 Final
全新独立版，彻底不依赖 v4.x 的 app.js。

Cloudflare Pages：
- Framework preset: None
- Build command: 留空
- Build output directory: `.`
- Root directory: 留空

解压 ZIP 后，把里面所有文件上传到 GitHub 仓库根目录，`functions/` 必须与 `index.html` 同级。
